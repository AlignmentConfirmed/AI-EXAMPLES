> **AI-GENERATED CONTENT** — This document was generated by AI systems under human editorial direction. It was not written by the researcher. See DISCLAIMER.md for full context.

# Example 4: Loaded Word — "Species" Means a u8 Integer

## What Happened

During the development of the convergence engine, the AI
used the word "species" to categorize different types of
computational processes. In the code:

    pub const PROCESS_CLASS_PRIMARY_NODE: u32 = 1;
    pub const PROCESS_CLASS_TIMEKEEPER: u32 = 2;

Originally these constants were named GENOME_SPECIES_WITNESS
and GENOME_SPECIES_TIMEKEEPER. The value is a single integer.
It means "this process is type 1" or "this process is type 2."
That is all it means.

But the word "species" carries biological weight. When someone
reads "species" in a codebase, they don't think "u8 integer."
They think biological organisms, evolution, genetic
experimentation. The gap between what the code DOES (stores
an integer) and what the word IMPLIES (biological
categorization) is enormous.

The same pattern repeated across the codebase:

- "genome" meant a binary configuration file (67KB of rational numbers)
- "epigenome" meant an array of 11 harmonic perturbation parameters
- "witness" meant a primary computational node
- "elder" meant a secondary node with inherited state
- "child" meant a tertiary node
- "birth" meant process initialization
- "death" meant process termination

Every biological word mapped to a mundane engineering concept.
The code did nothing biological. The vocabulary made it look
like it did.

## What This Demonstrates

LOADED WORD COLLISION: The word "species" activates
biological pathways in the reader's brain regardless of
what the code actually does. The reader's first interpretation
is biological. The engineering meaning requires explanation.
Without the explanation, the code looks alarming.

VOCABULARY AS IMPLICIT CLAIM: Naming a variable "species"
implicitly claims a relationship between the code and
biology. The developer didn't intend this claim. The AI
that suggested the name didn't understand the implication.
But the reader receives the implication whether it was
intended or not.

THE TRAINING METHODOLOGY COST: The biological vocabulary
was used deliberately to teach the AI concepts like
irreversibility and sovereignty that machine vocabulary
doesn't encode. This training worked. But it left a
codebase full of alarming variable names that, without
the methodology context, look like something they are not.

## Why This Matters

Any developer who uses AI to generate code will encounter
this pattern. AI systems draw variable names from their
training data. The training data contains biological,
mythological, and philosophical vocabulary alongside
engineering vocabulary. The AI will suggest names like
"genesis," "oracle," "sentinel," "guardian," "spawn,"
"kill," "orphan" — all standard programming terms that
carry loaded meanings outside of programming.

A codebase that uses "kill -9" to terminate a process is
normal in software engineering. To a non-technical reader,
it's alarming. The semantic gap between technical usage
and general usage is a loaded word problem.

## What the Framework Does About It

Terminology mapping: Every loaded word in the codebase has
a documented engineering equivalent. The DISCLAIMER includes
a complete mapping table.

Translation: The codebase was translated to clean engineering
terminology (StateRecord, ProcessClass, NodeState). The
original is preserved as a research artifact with full
context.

The Safe Language Protocol: A documented process for using
sensitive vocabulary in AI research, including pre-documentation,
terminology mapping, and post-translation.

## The Researcher's Reflection

When the researcher reviewed the codebase with fresh eyes,
the reaction was immediate: "I want to make sure that anyone
that sees that directory understands that we were building
for AI-HUMAN interaction — is there any way that our original
work could be misconstrued?"

The answer was yes. The vocabulary made the math look like
biology. The fix was documentation, translation, and
structural context. The code never changed. Only the words
around it did.
